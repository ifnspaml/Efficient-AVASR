# @package _global_

# Provisional clean alias. The launcher materializes the selected C/D run from
# immutable selection/d_to_e.json and reuses it when the digest is identical.
task:
  noise_prob: 0.0
  noise_snr: "0"
  noise_wav: null
  distillation_noise_prob: 0.0
  distillation_noise_snr: "0"
  distillation_noise_method: rms
  distillation_noise_manifest_root: null
  distillation_noise_train_only: true

model:
  distillation_noise_prob: 0.0
  distillation_noise_snr: "0"
  distillation_noise_method: rms
  distillation_noise_manifest_root: null
  distillation_noise_train_only: true
